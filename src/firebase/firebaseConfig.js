import { firebase } from "@react-native-firebase/database";

const firebaseConfig = firebase.app().database("https://chirkut-95cb1-default-rtdb.asia-southeast1.firebasedatabase.app/");

export default firebaseConfig;